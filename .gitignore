# Spieldaten des Nutzers duerfen niemals ins Repository gelangen.
build/
*.iso
*.gcm
*.rvz
*.wia
*.ciso
*.gcz
*.wbfs
main.dol
*.MAP
import.json

# Vom Recompiler erzeugter Modulcode (host-neutral, aber aus Spieldaten abgeleitet)
generated/
*.so
*.dll
*.dylib

# Externe Abhaengigkeiten (per Bootstrap geklont, nicht vendoriert)
ref/

# RVZ-Leser: auf Wunsch des Auftraggebers nicht im Repository.
# Ohne ihn lehnt tools/import komprimierte Container wie bisher mit Hinweis ab;
# gewandelt wird dann mit DolphinTool (siehe docs/HANDOFF.md, Abschnitt 5).
tools/import/rvz.py

# Werkzeuge
__pycache__/
*.pyc
.venv/
